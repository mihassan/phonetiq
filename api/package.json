{
  "name": "phonetiq-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "wrangler": "node ./scripts/wrangler-with-env.mjs",
    "dev": "npm run wrangler -- dev",
    "dev:v2": "npm run wrangler -- dev --port 8788 --var RECOGNITION_FOUNDATION_V2:true",
    "dev:twopass": "npm run wrangler -- dev --port 8789 --var EXPERIMENT_TWO_PASS:true",
    "deploy": "npm run wrangler -- deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval": "npx tsx scripts/run-eval.ts --delay-ms 7000 --label \"BASELINE (biasing prompt + fuzzy match)\"",
    "eval:v2": "npx tsx scripts/run-eval.ts --delay-ms 7000 --base-url http://localhost:8788 --label \"FOUNDATION-V2 (no biasing, strict match)\"",
    "eval:fast": "npx tsx scripts/run-eval.ts --delay-ms 0 --label \"BASELINE (biasing prompt + fuzzy match)\"",
    "eval:fast:v2": "npx tsx scripts/run-eval.ts --delay-ms 0 --base-url http://localhost:8788 --label \"FOUNDATION-V2 (no biasing, strict match)\"",
    "eval:twopass": "npx tsx scripts/run-eval.ts --delay-ms 7000 --base-url http://localhost:8789 --label \"E4 TWO-PASS (baseline + two-pass fallback)\"",
    "eval:fast:twopass": "npx tsx scripts/run-eval.ts --delay-ms 0 --base-url http://localhost:8789 --label \"E4 TWO-PASS (baseline + two-pass fallback)\"",
    "db:migrate:local": "npm run wrangler -- d1 migrations apply phonetiq-db --local",
    "db:migrate:remote": "npm run wrangler -- d1 migrations apply phonetiq-db --remote",
    "db:seed:local": "npm run wrangler -- d1 execute phonetiq-db --local --file=./src/db/seed.sql",
    "db:seed:remote": "npm run wrangler -- d1 execute phonetiq-db --local --file=./src/db/seed.sql"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260418.1",
    "drizzle-kit": "^0.31.10",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.83.0"
  }
}
